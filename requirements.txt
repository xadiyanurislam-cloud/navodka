flask>=3.0
requests>=2.31
pywebview>=5.0
dnspython>=2.6
openpyxl>=3.1
# Подделывает отпечаток TLS-рукопожатия. Без неё hh.ru отвечает 403
# на любые заголовки: он отсекает не по User-Agent, а раньше.
curl_cffi>=0.7
# Без неё requests не умеет socks5 и отвечает «Missing dependencies for
# SOCKS support». Прокси в России чаще всего дают именно socks5.
PySocks>=1.7
